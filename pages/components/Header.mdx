# Header

ウェブサイトの上部に表示されるナビゲーションバーで、ロゴ、メニューリンク、その他の重要な要素を含みます。

![Header](/header.png)



```js filename="layout.tsx"
import Header from "@/components/Header";

export default function Layout({ children }) {
  return (
    <>

      {children}
    </>
  );
}
```